export type EmployeeData = {
  label: string;
  num: number;
};

export type DomesticItem = {
  sku: string;
  itemName: string;
  amazonAsin: string;
  avgcost: number;
  avgSellingPrice: number;
};
